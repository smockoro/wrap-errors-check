sudo: false
language: go

env:
    - GO111MODULE=on

go:
    - 1.9.x
    - 1.10.x
    - 1.11.x

before_install:
    - go get github.com/mattn/goveralls

script:
    - go test -v
    - $GOPATH/bin/goveralls -service=travis-pro
    - go build -o main
